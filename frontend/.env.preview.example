# Preview deployments (Vercel's per-PR/per-branch environment). Point at a
# staging backend, not production, so preview builds can't accidentally
# hit real user data or send real Discord alerts.
NEXT_PUBLIC_API_URL=https://staging-api.yourdomain.com
